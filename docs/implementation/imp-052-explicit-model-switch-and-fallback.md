# IMP-052 — Explicit model switching and fallback rollback

## Status

Implementation in progress.

IMP-052 adds explicit user-controlled model switching and local fallback orchestration. It uses injected synthetic adapters in CI. No real runtime, real model, user-side offline test, runtime installation, model download, cloud request, credential retrieval, tool execution, or capability execution is in scope.

Closes #163.
