name: Diagnose IMP-041 coverage mypy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "IMP-041 coverage mypy diagnosis is complete."
