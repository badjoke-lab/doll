name: Format IMP-041 coverage tests

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/imp-041-format.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: imp-041-work-item-record
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --dev --locked
      - name: Format
        run: |
          uv run ruff check . --fix
          uv run ruff format .
          uv run ruff check .
      - name: Commit formatting
        run: |
          rm .github/workflows/imp-041-format.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Format IMP-041 coverage tests"
          git push origin HEAD:imp-041-work-item-record
