name: IMP-031 temporary formatter

on:
  push:
    branches:
      - imp-031-conversation-persistence

permissions:
  contents: write

jobs:
  format:
    if: github.event.head_commit.message != 'Format IMP-031 files'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: astral-sh/setup-uv@v6
      - run: uv sync --dev --locked
      - run: uv run ruff check --fix src/doll/state_repository.py tests/test_conversation_contracts.py tests/test_conversation_state_failures.py
      - run: uv run ruff format src/doll/state_repository.py tests/test_conversation_contracts.py tests/test_conversation_state_failures.py
      - name: Commit formatting
        run: |
          if git diff --quiet -- src/doll/state_repository.py tests/test_conversation_contracts.py tests/test_conversation_state_failures.py; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/doll/state_repository.py tests/test_conversation_contracts.py tests/test_conversation_state_failures.py
          git commit -m "Format IMP-031 files"
          git push origin HEAD:imp-031-conversation-persistence
